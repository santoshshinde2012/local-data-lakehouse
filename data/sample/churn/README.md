# Churn bronze samples

- `fixtures/tiny/` — 10-user demo (committed)
- `*.csv` here — generated by `make churn-sample` (default N=5000, gitignored)

```bash
make churn-sample
make churn-gold-local   # no Docker
# or: make up && make wait && make churn-e2e
```
